name: Lekhnath Action
run-name: Lekhnath CI/CD demo action 🚀
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
        uses: actions/checkout@v4
      - name: List files in the repository
        run: pytest

